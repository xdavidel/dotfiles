#!/usr/bin/env bash

[ "$(stat -c %y ~/.local/repo/corona 2>/dev/null | cut -d' ' -f1)" != "$(date '+%Y-%m-%d')" ] && curl curl -s https://corona-stats.online/Israel > ~/.local/repo/corona

grep "Total" ~/.local/repo/corona |
	sed "s/\s*//g;s/\x1b\[[0-9;]*m//g;s/│/;/g" |
	awk -F';' '{print "😷" $3 " (" $9") 💀" $5 " (" $7 "%)"}'
