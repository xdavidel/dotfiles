#!/usr/bin/env sh

! command -v fzf >>/dev/null && echo "fzf is not installed" && exit 127

find ~/.*profile ~/.tmux.conf ~/.*rc ~/.config/ ~/.local/scripts/ -type f \
	| fzf --preview='preview {}' \
				--bind='Ctrl-p:toggle-preview' \
	| xargs -r $EDITOR
