#!/usr/bin/env sh

if [ -z "$2" ]; then
	man -k "$1" | awk '{print $1}' | uniq | fzf -i --preview='man {}' | xargs -r man
else
	man -k "$1" | awk '{print $1}' | uniq | dmenu -i -l 20 | xargs -r man -Tpdf | ifne zathura -
fi
