#!/bin/sh
weatherfile="$HOME/.local/repo/weatherreport"
[ "$(stat -c %y $weatherfile | awk '{print $1}')" != "$(date '+%Y-%m-%d')" ] && getforecast

printf "%s" "$(sed '16q;d' $weatherfile | grep -wo "[0-9]*%" | sort -n | sed -e '$!d' | sed -e "s/^/ /g" | tr -d '\n')"

sed '13q;d' "$weatherfile" | grep -o "m\\(-\\)*[0-9]\\+" | sort -n -t 'm' -k 2n | sed -e 1b -e '$!d' | tr '\n|m' ' ' | awk '{print " ",$1 "°","",$2 "°"}'
